public class Notebook extends Device{
    public Notebook(int id, double price, double discount, int stock, String name, String brand, int storage, int ram, double screenSize) {
        super(id, price, discount, stock, name, brand, storage, ram, screenSize);
    }
}
